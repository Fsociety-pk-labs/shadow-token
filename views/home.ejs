<div class="home-container">
    <div class="hero-section">
        <div class="glitch-text" data-text="SHADOW">SHADOW</div>
        <p class="company-status">NETWORK STATUS: <span class="status-active">ACTIVE</span></p>
    </div>

    <div class="main-content">
        <div class="terminal-window">
            <div class="terminal-header">
                <span class="terminal-title">shadow@network:~#</span>
                <span class="terminal-controls">
                    <span class="control-dot"></span>
                    <span class="control-dot"></span>
                    <span class="control-dot"></span>
                </span>
            </div>
            <div class="terminal-body">
                <p>Welcome to Shadow IT Solutions</p>
                <p>Enterprise Infrastructure & Cloud Integration Platform</p>
                <p class="text-secondary">>_ System initialized</p>
                <p class="text-secondary">>_ Security audit in progress...</p>
                <p></p>
                <p>This is a secure network. Unauthorized access is prohibited.</p>
                <p class="text-secondary">>_ Type 'help' for available commands</p>
            </div>
        </div>

        <div class="message-box">
            <p>⚠️ <strong>NOTICE:</strong> This system contains sensitive data. Only authorized personnel may access internal services.</p>
            <p style="margin-top: 10px; font-size: 0.9rem; color: var(--text-secondary);">
                If you believe you have authorization, investigate the network topology and locate the entry points.
            </p>
        </div>

        <div class="footer-info">
            <p>Last system check: 2024-01-15 09:32:00 UTC</p>
            <p>All activities are logged and monitored</p>
        </div>
    </div>

    <!-- Hidden hints in source code for curious investigators -->
    <!-- 
    SHADOW NETWORK - SECURITY AUDIT LOG
    
    Hint 1: Check the server's public configuration files. Webmasters often leave clues in plain sight.
    
    Debug: Different microservices are exposed at:
    - /diagnostic-status (requires proper auth token)
    - /login (employee portal)
    - /profile (session management)
    - /sso (identity provider)
    
    Warning: Some endpoints may contain Base64 encoded credentials in HTTP headers.
    The JWT tokens can be inspected using browser DevTools (F12 -> Application -> Cookies/Storage)
    
    Known vulnerabilities being tested:
    CVE-2015-9235 (JWT alg:none bypass) - check /sso endpoint
    Weak session tokens - check /profile endpoint
    Information disclosure - look for robots.txt or sitemap
    
    For penetration testers: decode base64 strings found in network requests
    -->
</div>

<style>
    .home-container {
        text-align: center;
        display: flex;
        flex-direction: column;
        gap: 25px;
    }

    .hero-section {
        text-align: center;
        margin-bottom: 10px;
    }

    .glitch-text {
        font-size: 3.5rem;
        font-weight: 900;
        letter-spacing: 3px;
        background: linear-gradient(90deg, var(--accent-cyan), var(--accent-purple));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        position: relative;
        animation: glitch 0.3s infinite;
        text-transform: uppercase;
    }

    @keyframes glitch {
        0% { transform: translate(0); }
        20% { transform: translate(-2px, 2px); }
        40% { transform: translate(-2px, -2px); }
        60% { transform: translate(2px, 2px); }
        80% { transform: translate(2px, -2px); }
        100% { transform: translate(0); }
    }

    .company-status {
        font-size: 0.9rem;
        letter-spacing: 2px;
        margin: 15px 0 0 0;
        color: var(--text-secondary);
        font-family: var(--font-mono);
    }

    .status-active {
        color: #00ff41;
        animation: pulse 2s infinite;
    }

    @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.5; }
    }

    .main-content {
        display: flex;
        flex-direction: column;
        gap: 25px;
        align-items: center;
    }

    .terminal-window {
        width: 100%;
        max-width: 600px;
        background: rgba(0, 0, 0, 0.6);
        border: 2px solid var(--accent-cyan);
        border-radius: 8px;
        box-shadow: 0 0 30px rgba(0, 240, 255, 0.2), inset 0 0 20px rgba(0, 240, 255, 0.05);
        overflow: hidden;
        animation: panelscan 4s linear infinite;
    }

    @keyframes panelscan {
        0%, 100% { box-shadow: 0 0 30px rgba(0, 240, 255, 0.2), inset 0 0 20px rgba(0, 240, 255, 0.05); }
        50% { box-shadow: 0 0 50px rgba(0, 240, 255, 0.4), inset 0 0 30px rgba(0, 240, 255, 0.1); }
    }

    .terminal-header {
        background: linear-gradient(90deg, rgba(0, 240, 255, 0.1), rgba(176, 38, 255, 0.1));
        border-bottom: 1px solid var(--border-color);
        padding: 12px 15px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .terminal-title {
        font-family: var(--font-mono);
        color: var(--accent-cyan);
        font-size: 0.9rem;
        font-weight: 600;
    }

    .terminal-controls {
        display: flex;
        gap: 8px;
    }

    .control-dot {
        width: 12px;
        height: 12px;
        border-radius: 50%;
        background: var(--accent-cyan);
    }

    .terminal-body {
        padding: 20px;
        font-family: var(--font-mono);
        font-size: 0.9rem;
        line-height: 1.8;
        text-align: left;
    }

    .terminal-body p {
        margin: 8px 0;
        color: #00ff41;
    }

    .text-secondary {
        color: var(--accent-purple) !important;
    }

    .message-box {
        width: 100%;
        max-width: 600px;
        background: rgba(255, 193, 7, 0.05);
        border: 2px solid #ffc107;
        border-radius: 8px;
        padding: 20px;
        color: #ffc107;
        font-size: 0.95rem;
        text-align: left;
    }

    .message-box p {
        margin: 8px 0;
    }

    .footer-info {
        font-size: 0.85rem;
        color: var(--text-secondary);
        font-family: var(--font-mono);
    }

    .footer-info p {
        margin: 4px 0;
    }
</style>
